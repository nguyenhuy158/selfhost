# Netdata Cloud claim — lấy ở https://app.netdata.cloud, phần "Connect Nodes".
NETDATA_CLAIM_TOKEN=
NETDATA_CLAIM_URL=https://app.netdata.cloud
# UUID của room muốn gắn node vào. Để trống thì node vào room mặc định.
NETDATA_CLAIM_ROOMS=
