services:
  commafeed:
    container_name: commafeed
    image: athou/commafeed:latest-h2
    restart: unless-stopped
    volumes:
      - ./db:/commafeed/data
    deploy:
      resources:
        limits:
          memory: 256M
    ports:
      - 8282:8082

