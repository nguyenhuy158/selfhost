# Token của Cloudflare Tunnel: dashboard Zero Trust -> Networks -> Tunnels ->
# chọn tunnel -> Install connector, token nằm trong lệnh mẫu.
#
# Token này KHÔNG hết hạn và cho phép dựng tunnel vào mạng nội bộ. Lộ ra là
# phải rotate ngay ở dashboard (tunnel cũ mất hiệu lực).
CLOUDFLARE_TUNNEL_TOKEN=
