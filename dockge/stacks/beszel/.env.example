# Token của agent, lấy ở UI Beszel: Add System -> phần token hiện ra.
# KEY trong compose.yaml là khoá CÔNG KHAI của hub, không phải secret,
# nên để thẳng trong compose, không đưa vào đây.
BESZEL_AGENT_TOKEN=xxxx-xxxxxxxx-xxxx-xxxxxxxxxxxx
